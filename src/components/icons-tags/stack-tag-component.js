const StackTag = ({ children }) => {
	return (
		<span className='mr-2 lg:mr-0 mb-2 lg:mb-0 px-3 py-1 bg-layout/30 text-accent text-sm rounded-full '>
			{children}
		</span>
	);
};

export default StackTag;
